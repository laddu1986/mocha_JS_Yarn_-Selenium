image: 
  name: massiveinteractive/qa-ci
  username: $docker_user
  password: $docker_user_pwd
options:
  docker: true
pipelines:
  branches:
    update-pipelines:
      - step:
          script:
            - yarn install
            - yarn run api
            - sh ./scripts/start.sh
            - yarn install
            - yarn run web