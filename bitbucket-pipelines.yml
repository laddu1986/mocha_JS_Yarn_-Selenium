image: 
  name: massiveinteractive/qa-ci
  username: $docker_user
  password: $docker_user_pwd
options:
  docker: true
pipelines:
  branches:
    update-pipelines:
          - step:
             caches:
              - docker
              - node
             script:
              - sh ./scripts/start.sh
              - yarn install
              - yarn run web