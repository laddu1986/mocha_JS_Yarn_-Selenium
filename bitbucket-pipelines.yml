image: qamassive/v2
options:
  docker: true
pipelines:
  default:
    - step:
        script:
          - sh ./scripts/start.sh
          - netstat -plnt
          - npm install
          - npm run web
